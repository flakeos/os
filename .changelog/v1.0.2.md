- rename src/modules to src/module and move tests/config/scripts/docs into src/
- rename maclike module to layout (default on workstation)
- package split ISOs in zip with join.sh reassembly script
- split desktop ISO >2GB into parts for GitHub release
- correct PROJECT_DIR path in iso-build.sh after restructure
- rename nftables config from .nix to .conf to fix statix syntax error
