refactor(module): rename src/modules to src/module and move tests/config/scripts/docs into src/
fix(release): split desktop ISO >2GB into parts for GitHub release
fix(build): correct PROJECT_DIR path in iso-build.sh after restructure
